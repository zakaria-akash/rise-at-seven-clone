// Services list — referenced in page.js but section is not prominently shown on homepage.
// Keeping as a minimal pass-through so no import errors occur.
export default function ServicesList() {
  return null;
}
